% Play using and stoping using Lua modules, perhaps by adding and removing packages that use Lua modules, such as fontspec, polyglossia or microtype

% These packages cannot be loaded in the dumping session because of Lua. At least, the Lua modules they require will be preloaded in the format.
\usepackage{fontspec}
\usepackage{microtype}

\begin{document}%
	\fontspec{Latin Modern Roman}%
	\lipsum%
\end{document}